[project]
name = "atlas-schemas"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.6",
]

[tool.setuptools.packages.find]
include = ["atlas_schemas*"]
exclude = ["node_modules*"]
