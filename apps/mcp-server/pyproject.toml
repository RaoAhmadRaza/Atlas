[project]
name = "atlas-mcp-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastmcp>=0.1",
  "httpx>=0.27",
  "pydantic>=2.6",
  "atlas-core",
]

[tool.uv.sources]
atlas-core = { workspace = true }
